service 接口实现类中调用
ApplicationContext  ac=
new ClassPathXmlApplicationContext("applicationContext.xml");

然后调用接口 UserDao  ud=ac.getBean(UserDao.class);

Street s=ul.findByid(id);

return s;

把最底层dao 写入application 配置上 
记得（daoimpl里面属性SQLSessionFactory）

Daoimpl 里面 new 取出getMapper();
应该是接口的 Street 调用接口的findById();


application.xml中要配置 UserDao.xml
 <bean id="sqlSessionFactory"
class="org.mybatis.spring.SqlSessionFactoryBean"
p:configLocation="classpath:mybatis-config.xml"	
p:dataSource-ref="dataSource"	
p:mapperLocations="classpath:com/bw/test/mybatis/dao/StreetDao.xml"/>
		  配置了StreetDao.xml 就不用在mybatis里面配置了。